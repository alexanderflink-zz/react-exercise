import React from 'react';
import css from './BuildControls.css';

const buildControls = (props) => {
  return (
    <div className={css['build-controls']}></div>
  );
};

export default buildControls;
